# 船種・造船所 追加設計書

状態：実装済み。実装仕様はREADME.md・design.mdにも反映済み。

本書は船種の追加と、船取引・艤装を統合する造船所の実装基準をまとめる。プレイヤー向けの説明はREADME.md、実装の構成はdesign.mdを参照する。既存の探索・海図・艤装についてはexpansion-design.mdを参照し、本書で変更する船の扱いを優先する。

## 1. 目的と範囲

- 従船に8船種を導入し、容量と船団共通バフで選択肢を作る。
- 中央の地図カードを置き換える既存艤装画面を「造船所」へ拡張する。街でのみ取引・艤装変更が可能。
- 船取引、容量計算、維持費、戦闘、報酬、喪失、詳細表示、保存復元、デバッグを一貫した船種データへ接続する。
- 船は船種別の隻数で管理する。個体名、耐久、個別装備、乗員割当は追加しない。
- 聖船は従来の基本容量を担う存在として別扱いとする。船種別の売買・抽選・喪失・バフ隻数に含めない。初期の従船は0隻。
- 出撃上限は従来どおり20部隊・200人。部隊上限の増加は控えを含む保有人数の上限に適用する。

## 2. 船種の確定値

| 内部ID案 | 船種 | 購入価格 | 物資容量加算 | 部隊容量加算 | 容量価値 | 1隻の固有効果 | 船種バフ上限 |
| --- | --- | ---: | ---: | ---: | ---: | --- | --- |
| caravel | キャラベル | 4,000 | 35 | 10 | 65 | 部隊維持費－2% | 5隻で－10% |
| knarr | クナール | 4,500 | 45 | 10 | 75 | 物資上限＋2% | 5隻で＋10% |
| longship | ロングシップ | 5,000 | 30 | 15 | 75 | 部隊ATK＋2% | 5隻で＋10% |
| cog | コグ | 5,500 | 75 | 10 | 105 | なし | なし |
| galley | ガレー | 6,000 | 25 | 25 | 100 | 部隊上限＋2.5% | 4隻で＋10% |
| carrack | キャラック | 10,000 | 90 | 30 | 180 | なし | なし |
| galleass | ガレアス | 12,000 | 55 | 35 | 160 | 支援射撃威力＋10%、砲撃支援の間隔－1tick | 2隻で＋20%、－2tick |
| galleon | ガレオン | 15,000 | 80 | 35 | 185 | 部隊DEF＋5% | 2隻で＋10% |

容量価値＝物資容量加算＋部隊容量加算×3。設計用の比較値であり、ゲーム画面の性能欄には表示しない。

バフが上限に達しても各船の固定容量は隻数分すべて加算する。船団の組み合わせは自由で、各船種のバフ上限は独立。上限を超えた保有隻数に追加バフは付けない。

## 3. 効果計算

### 3.1 適用範囲と丸め

- 全船種のバフは船団共通。部隊のATK・DEFと支援射撃は陸戦・海戦の両方へ適用する。
- 同種の艤装効果と船種バフは加算する。乗算で重ねない。
- ガレーの2.5%などの小数は効果集計中に切り捨てない。
- 物資基本容量60、部隊基本容量30は既存値を使用する。

物資上限＝floor((基本容量60＋全船の物資容量加算合計)×(100＋クナールの増加率＋艤装の物資増加率)/100)

部隊上限＝floor((基本容量30＋全船の部隊容量加算合計)×(100＋ガレーの増加率＋艤装の部隊増加率)/100)

維持費＝floor(全兵員の維持費合計×(100－キャラベルの軽減率－艤装の軽減率)/100)

維持費の軽減率は既存の安全処理に合わせて0～100%に制限する。兵種・部隊ごとに割引や丸めを行わない。

ATK・DEFは、既存の人数・レベル補正後に、船種と艤装の共通増加率・対象兵種への増加率を合算して適用する。整数化・地形補正の順序は既存処理を維持する。ロングシップは全部隊のATK、ガレオンは全部隊のDEFへ加算する。

例：キャラベル5隻と維持費－10%艤装なら合計－20%。ロングシップ5隻とATK＋5%艤装なら合計＋15%。

### 3.2 ガレアス

- 有効隻数＝min(保有ガレアス数, 2)。
- モリ投擲：威力30→33→36。間隔は5tickのまま。
- バリスタ：威力100→110→120。間隔は10tickのまま。
- 砲撃支援：間隔20→19→18tick。確定1部隊壊滅の効果はそのまま。
- 最初の発射も補正後の間隔で行う。2隻なら18・36・54tickに砲撃支援が発動する。
- 支援射撃の威力倍率はDEF軽減前に一度だけ適用する。最終ダメージの丸めは既存の共通処理を使う。
- ロングシップの部隊ATK上昇は、モリ・バリスタなどの固定威力へ適用しない。
- 対応する支援射撃艤装が未装備なら、新たな射撃は発生しない。「対応艤装未装備」と詳細に表示する。

### 3.3 戦闘中の固定

艤装と船の効果は開戦時にまとめて固定する。準備中の能力表示も同じ計算を使う。戦闘報酬で得た船や戦後の船団変更が、終了した戦闘・その支援効果へ遡って適用されないようにする。

## 4. 造船所の在庫

### 4.1 定番4種

街ごとに以下の価格帯から重複なしで定番を抽選する。各価格帯内の抽選は均等とする。

| 価格帯 | 候補 | 定番枠 | 補充目標数の初期案 |
| --- | --- | ---: | ---: |
| 低価格 | キャラベル、クナール、ロングシップ、コグ | 2 | 各2～3隻 |
| 中価格 | ガレー、キャラック | 1 | 1～2隻 |
| 高価格 | ガレアス、ガレオン | 1 | 1隻 |

4種類は確定仕様。補充目標数は先行提案に基づく調整可能な初期値で、実装時に定数化する。街の初期化時に船種と補充目標数を決め、セーブへ保存する。目標数自体は毎季節再抽選しない。

占領で所属勢力が変わっても定番・在庫は維持する。村には造船所在庫を生成しない。

### 4.2 補充

- 1季節30日、毎季節1日に補充する。「毎月」はこの処理を指す。
- 定番は在庫＝max(現在在庫, 補充目標数)へ更新する。現在庫への加算ではない。
- 目標を超える在庫は減らさない。
- 最終補充季節を保存し、同じ季節の再入場・再描画・再読み込みで重複補充しない。
- 初期化時は目標数で開始する。未初期化の旧セーブ街も一度だけ生成する。

### 4.3 買い取り在庫

- 全船種を、定番かどうかに関係なく街へ売却できる。
- 売却分は当該船種の街の在庫へ加算する。
- 定番外も在庫がある間は購入一覧に表示する。一時的に5種以上になってよい。
- 定番外は季節更新で補充せず、残存在庫を保持する。売り切れたら購入一覧から消す。
- 定番外は「買取在庫」と表示する。通常在庫との別通貨・別価格・二重の数量管理は作らない。
- 実装を簡単にするための季節更新時の消滅もユーザーから許容されているが、本設計では保持を採用する。変更する場合は文書と説明を合わせる。

## 5. 取引

- 購入単価は船種表の価格。売却単価はfloor(購入単価×SHIP_SELL_RATE)。初期値0.8を共通定数で管理する。
- 買い取り在庫を買い戻す場合も購入価格を支払う。
- 船種1種類・購入または売却の片方向を、数量指定して確定する。
- 購入は正の整数数量・在庫・所持資金、売却は正の整数数量・船種別所持数を確定直前にも再検証する。
- 成立時に資金・船団・街在庫を一括更新し、画面と保存へ反映する。二重押下による重複取引を防ぐ。
- 容量超過を理由に売却を禁止しない。売却前に物資・兵員それぞれの超過量を予告する。
- 売却・喪失によって容量超過しても、物資・兵員を自動で削除しない。整理は従来の売却・破棄・解雇を使う。

## 6. UI

### 6.1 画面構成

中央カードのタイトルを「造船所」とし、ヘッダは「艤装変更／船取引／詳細確認／地図に戻る」。船取引は艤装変更の隣に置く。

- 街の行動欄は造船所への入口へ統合する。初期表示は既存方針を引き継いで艤装変更。
- 艤装変更は既存の装備・保管・交換操作を維持する。
- 船取引には「購入／売却」切替を設ける。初期は購入。
- 詳細確認は現在の全数値に、船種別所持数、船団バフ内訳、艤装との合計、支援射撃の威力・間隔を追加する。
- 街外で既存の詳細閲覧導線を使う場合、効果の閲覧だけを許可し、取引・艤装変更は許可しない。

### 6.2 船取引の一覧と詳細

横長画面は左一覧・右詳細。地図と同じカード幅を維持し、一覧と詳細を独立スクロール可能にする。

左一覧：船アイコン、船種名、単価、在庫、所持数、固定容量の短い説明、選択状態。購入は定番4種＋買取在庫、売却は所持船種を表示。定番の在庫0は売切れ表示で残す。購入一覧は価格昇順、同額なら定義順とする。

右詳細：船アイコン、名称、単価、1隻の固定容量、固有バフ・上限、所持数・在庫、数量入力、取引総額、変更予告、確定ボタン。変更予告には次を含める。

- 資金、対象船の隻数。
- 物資上限・部隊上限の現在値→取引後。
- 実際の季節維持費と、今回変わるATK・DEFの増加率。
- 支援射撃の威力・間隔が変わる場合はその前後。
- 容量超過が発生する場合の物資・兵員超過量。

バフの上限到達を明記し、追加購入でも固定容量は増えることが分かるようにする。数値予告は取引後状態を複製して共通計算で求め、予告のために実状態を変更しない。

### 6.3 縦長スマホ

ヘッダボタンは折り返し可能にする。船一覧をコンパクトな選択カードとして上部、詳細・数量指定・確定を下部へ配置する。買取在庫で種類が増えても一覧だけが画面を占有しないよう制限し、一覧をスクロールさせる。

詳細領域は既存の艤装画面の改善を引き継ぎ、目安として最低360pxを確保する。画面高が不足する場合はカード全体の縦スクロールを許可し、詳細を小さな領域に押し潰さない。横長画面の高さ配分には影響させない。

### 6.4 表示上の補足

- ヘッダの従船数は船種別所持数の合計を表示する。聖船の扱いは変えない。
- 船アイコンは既存の線画・配色へ合わせる。新規船種アイコンの最終絵柄は実装時の制作対象。
- 容量価値、内部ID、抽選重みなどはプレイ画面に出さない。
- 既存の船取引モーダル・イベント配線は統合後に整理し、同じ取引を二つのUIから実行する状態を残さない。

## 7. 報酬・イベント・喪失

### 7.1 船を得る場合

- 既存の船を得る確率・隻数は維持し、獲得が決まった船の種類だけを共通抽選する。
- 船種の重みは1/購入価格。SHIP_REWARD_WEIGHT_EXPONENTの初期値1を用い、重み＝価格^(-指数)とする案で調整可能にする。高価なほど出にくい。
- 複数隻は1隻ごとに独立抽選し、同種重複を許可する。街の在庫には依存・影響しない。
- 隠れた入り江も共通抽選。3枚海図で2隻、5枚海図で4隻。高価格帯の保証は設けない。
- 「ランダム」は前段で合意した共通の価格反比例抽選を指し、入り江だけ8種均等にはしない。
- 結果は「コグ＋1隻、ガレー＋1隻」のように種類別に集約し、ダイアログで確認できるようにする。
- 抽選結果は報酬を具体化した時点で保存し、再描画・確定の再試行で船種が変わらないようにする。報酬の付与も一度だけ行う。
- 旧保存報酬が隻数だけを持つ場合は、一度だけ船種抽選して結果を保存してから付与する。

### 7.2 船を失う場合

- 保有船を購入価格の安い順に減らす。同額なら定義順とする。ランダム抽選しない。
- 複数隻の喪失は最安船種を使い切ってから次へ進む。所持数以上は失わない。
- 結果に失った船種と隻数を表示する。売却収入は発生しない。
- 現行に船喪失経路がない場合でも共通処理として用意し、船数の直接減算を新設しない。新たな喪失イベント自体は本機能の範囲外。

## 8. 状態・保存・デバッグ

実装用の状態案はstate.fleet＝{ version: 1, counts: { cog: 0, ... } }。船種別隻数を唯一の正とし、合計隻数は共通関数で算出する。既存state.shipsと両方を個別更新する設計は避ける。

街にはshipyard＝{ regular: [{ type, targetStock }], stock: { 船種ID: 数量 }, lastRestockSeason }を保存する案。世界の保存・復元に必ず含める。

- 旧セーブのstate.shipsは同数のコグへ変換する。新形式がある場合はそちらを優先し、再変換・二重加算しない。
- 基本はリセット推奨だが、旧セーブも読み込み可能にする。コグ変換で兵員上限が下がっても自動で兵を失わない。
- 船団・在庫は非負の安全な整数へ正規化し、不明船種は効果計算へ混入させない。
- デバッグ入力は「コグ数」に変更し、コグだけを設定する。他船種の隻数は維持する。合計従船数は別途表示する。
- デバッグ変更でも容量・維持費・艤装詳細・ヘッダを共通計算で更新する。
- 補充・報酬・売買・デバッグは既存の保存処理へ接続し、読み込みだけで抽選がやり直されないようにする。

## 9. 実装の責務分割と接続先

ファイル名は案。実装時は既存命名に合わせ、画面・状態変更・純粋計算を分ける。

| 担当案 | 責務 |
| --- | --- |
| shipConfig.js | 8船種、バフ上限、売却率、品揃え価格帯、補充数、抽選設定 |
| fleet.js | 船団の正規化、総数、船種バフ、獲得・最安順喪失、報酬船種抽選 |
| shipyard.js | 街の品揃え初期化、季節補充、売買検証・一括更新 |
| shipyardUI.js | 船取引の一覧、数量指定、詳細と変化予告 |
| outfitting.js等の共通効果層 | 船と艤装の合算、容量・維持費・戦闘スナップショット |

現行コードの接続確認箇所：

- state.js、storage.js、map.js：初期船団、旧セーブ移行、街在庫の世界保存。
- supplies.js、troops.js：現在の隻数×固定容量を船種別容量へ変更。
- upkeep.js、outfitting.js、outfittingUI.js、battle.js：予告・実消費・実戦能力を同じ効果へ接続。
- marketUI.js、index.html、dom.js、styles.css：旧船取引の撤去・造船所への統合。
- actions.js、ui.js、quests.js：イベント報酬・戦利品・依頼報酬のstate.ships直接加算を共通獲得処理へ変更。
- exploration.js、explorationUI.js、charts.js、chartUI.js等：船報酬の抽選時点・保存・付与と表示を揃える。
- panelUI.js、ui.js：ヘッダ合計とデバッグのコグ数。
- 日付進行処理：季節境界の在庫補充。既存の物資・雇用更新を変更しない。

全ファイルのships参照・代入と、報酬内のshipsフィールドを実装時に再検索する。上記の一覧だけで網羅済みと判断しない。

## 10. 検証条件

- 8船種の価格・容量・バフが表と一致する。
- 0隻、上限直前、上限到達、上限超過で固有バフが正しい。固定容量は上限後も増える。
- ガレー1隻の2.5%が失われず、船団全体と艤装の容量が最後に一度だけ丸められる。
- キャラベルと艤装の維持費軽減が合計値へ一度だけ適用され、予告と実支払いが一致する。
- モリ30/33/36、バリスタ100/110/120、砲撃20/19/18tickと、開戦時固定を確認する。
- 各街の定番は4種、価格帯は2/1/1。保存復元・季節更新で定番が変わらない。
- 補充は目標数までで、過剰在庫を減らさず、同季節の再実行でも増殖しない。
- 定番外売却、買い戻し、売切れ非表示、季節を越えた保持を確認する。
- 不足資金、不足在庫、不正数量、二重確定で資金・船・在庫に部分更新が起きない。
- 容量超過を伴う売却・喪失・移行で兵員や物資が自動削除されない。
- 報酬の共通重み抽選、複数隻、船種の保存、二重付与防止、最安順喪失を確認する。
- 旧shipsのコグ移行は一度だけで、デバッグ変更が他船種を上書きしない。
- PCと縦長スマホで一覧の切替による幅揺れがなく、詳細・数量・確定操作が十分な高さで使える。

## 11. 実装の分割案

1. 設定・船団状態・移行・共通効果計算とテスト。
2. 全獲得経路・容量・維持費・戦闘・詳細・デバッグの接続。
3. 街在庫・季節補充・売買処理。
4. 造船所UI統合、PC・スマホ確認、旧UI整理。
5. プレイ検証とREADME.md・design.mdへの実装済み仕様反映。

必須の追加質問はなし。補充目標数の範囲など本書で「初期案」とした調整値は、実装時に設定へ集約し、プレイ後に変更できるようにする。
