# 国力システム設計書

状態：実装済み。データ層・ゲーム進行への反映・勢力シート・軍資金提供を接続。

実装範囲：共通定数と保存復元に加え、日次回復・消耗・戦況補正、依頼／戦闘報酬、拠点喪失、軍資金提供、勢力シートの内訳表示。`tests/nationalPower.test.cjs` で日次順序・重複防止・占領・提供の確定と保存失敗時の復元を、保存テストで旧セーブ補完と値の維持を検証する。

本書は合意済みの国力仕様と処理方針をまとめる。数値は暫定で、共通定数から後日調整できる。README.md・design.mdには利用方法と構成の要点を反映する。

## 1. 位置づけ・対象

- 国力は勢力が戦争を継続する余力を表す数値。戦況とは独立して保持し、開戦・終戦でリセットしない。
- 北海連合・群島同盟・湾岸城塞群の国家3勢力を対象とする。海賊、無所属プレイヤーには作らない。
- 全勢力で上限を共通化し、拠点数は季節回復量へ反映する。国力の上限を拠点数には連動させない。
- 国力差による日次の戦況補正を通じて、前線決着へ間接的に影響させる。兵数・兵種・兵員能力・戦闘報酬の名声スケールは変更しない。
- 国力0だけでは滅亡・敗戦・拠点喪失にしない。回復や支援で立て直せる。
- 名誉家臣は謁見中に軍資金を提供できる。通常依頼や戦時の活動も国力へ反映する。

## 2. 共通設定と暫定値

`nationalPowerConfig.js` の `NATIONAL_POWER_CONFIG` に集約する。UI・保存補完・増減計算・日次処理で別々に数値を持たない。

| 設定項目 | 暫定値 | 意味 |
| --- | ---: | --- |
| initial | 600 | 新規ゲーム・国力のない旧セーブの初期値 |
| max | 1,000 | 全国家共通の上限 |
| recovery.village | 5 | 村1か所につき毎季節回復 |
| recovery.town | 10 | 街1か所につき毎季節回復 |
| questGain | 1 | 通常依頼・平時にも受けられる貴族依頼の達成 |
| warActionGain | 2 | 前線行動・戦時依頼の支援側増加 |
| warActionLoss | 2 | 同活動の敵側減少 |
| regularVictoryGain | 2 | 交戦中の正規軍撃破による所属勢力増加 |
| regularVictoryLoss | 2 | 同戦闘による敵勢力減少 |
| dailyDefenseCost | 0.2 | 防衛中の前線1件につき毎日消耗 |
| dailyAttackCost | 0.4 | 攻撃中の前線1件につき毎日消耗 |
| settlementLoss.village | 40 | 村を奪われた時の減少 |
| settlementLoss.town | 80 | 街を奪われた時の減少 |
| donationFundsPerPoint | 1,000 | 国力1を増やすのに必要な資金 |
| warBias.referenceDifference | 500 | 補正上限に到達する国力差 |
| warBias.maxDaily | 0.2 | 交戦ペアごとの日次戦況補正の絶対値上限 |
| precision | 1,000 | 国力1に対する内部整数単位数 |

設定対象勢力は内部IDで明示する。表示名や日本語の文字列比較で判定しない。

街の回復は村の2倍を維持する。設定の検証でこの関係を確認するか、街の回復値を村の値と倍率2から導出する。

数値例：村10・街5で季節回復100。攻撃2件・防衛2件を30日続けると消耗36。国力の高低だけで勝敗が固定されず、拠点喪失やプレイヤー支援が意味を持つかを試遊で確認する。

## 3. 精度・表示・上限

- 国力は0～maxへ制限する。内部では0.001単位の整数で保持し、0.2の日次消耗や任意額の提供で浮動小数の誤差が蓄積しないようにする。
- 汎用増減関数は要求量と実増減量を区別し、報告には上限・下限適用後の実増減量を使用する。
- 通常の国力表示は小数第1位までを基本とする。細かな軍資金提供の予告・結果は必要に応じて小数第3位まで表示し、効果があるのに「＋0」と見せない。
- 上限に達している勢力の回復・依頼による増加は切り捨てる。超過分の繰越は行わない。
- 敵側が0でも味方側の増加を減らさず、味方側が上限でも敵側の減少を止めない。国力は敵からの移転ではない。
- precisionは保存形式の一部として扱う。ゲームバランス用の定数とは区別し、変更する場合は保存バージョン移行を伴わせる。

## 4. 季節回復・前線消耗

### 4.1 季節回復

- 既存の暦に合わせて毎季節1日、現在所有する村と街から回復量を計算する。平時・戦時ともに実行する。
- 回復量＝村数×5＋街数×10。拠点が0なら自然回復も0。
- 占領後は新所有者が次の季節の回復を受ける。季節途中の所有変更による日割り回復は行わない。
- 新規開始日は国力600で開始し、開始時の春1日だからという理由では追加回復しない。

### 4.2 前線消耗

- 有効な未決着前線1件につき、攻撃側−0.4、防衛側−0.2を毎日適用する。
- 同じ勢力が複数前線に参加する場合は合計する。対戦国が複数でも、勢力全体の一つの国力から支払う。
- 同じ前線を両国の台帳から二重に数えない。既存の1拠点1前線の整合性確認後に集計する。
- 防衛成功時に拠点喪失ペナルティは発生しない。攻撃失敗の追加ペナルティも今回は設けず、日次の攻撃コストを負担とする。

### 4.3 日次の順序

1. 既存の前線整合性確認を行い、その日の処理開始時点の所有拠点・有効前線を確定する。
2. 季節境界なら所有拠点に応じた回復を行い、上限を適用する。
3. 開始時点の有効前線の消耗を全勢力分まとめて適用する。
4. この時点の全国家の国力から、全交戦ペアの日次補正を計算・適用する。
5. 既存の前線決着・終戦・新規前線開始を実行する。拠点喪失の減少もここで適用する。

その日に新設された前線は翌日から消耗する。その日に決着する既存前線は当日分を消耗する。決着による国力減少が、台帳の処理順に応じて別の戦争の当日補正を変えないようにする。季節1日に決着する拠点は、その日の回復処理時点の所有者に回復を与える。

複数日を一度に進める場合も1日ずつこの順序で処理する。台帳が空でも季節回復は実行し、既存 `tickDailyWar` の早期returnに巻き込まれないよう責務を分離する。

## 5. 戦況への間接的な影響

現行の前線は勝率抽選ではなく、全体戦況と局地スコアから閾値到達または期限時点で決着する。この判定を維持する。

交戦ペアをA・Bとした時：

`A側の日次戦況補正 = clamp((国力A − 国力B) / referenceDifference, −1, 1) × maxDaily`

- 国力差0なら補正なし。Aが250上回ればA側に＋0.1/日、500以上なら＋0.2/日。
- 既存台帳の `factions[0]` を正方向として符号を合わせる。両側からそれぞれ二重に加算しない。
- 現在交戦中の国家同士だけを対象とする。海賊・無所属・和平後の台帳には適用しない。
- 前線の局地スコアや勝率には追加補正しない。既存の全体戦況を通じて間接的に効かせる。
- 1ペアで前線が何件あっても戦況補正は1日1回。同時に複数国と交戦している場合は各ペアで計算する。
- 国力差の補正は日次だけ。依頼達成や提供の度にその場で戦況を追加更新しない。
- 小数の戦況補正を切り捨てず保持する。既存戦況の表示・閾値判定が小数を許容するか確認し、国力由来の微小値だけで毎日通知を出さない。

## 6. 活動による国力変化

### 6.1 支援する勢力

- 依頼は受注時に依頼主側の勢力を固定する。配達でも加算先は受注元とし、配達先の勢力には加算しない。
- 貴族依頼は依頼主の貴族の所属勢力を使用する。受注後の占領によって別勢力へ付け替えない。
- 前線活動は実際に支援した側を使用し、無条件にプレイヤー所属へ加算しない。
- 通常戦闘の正規軍撃破だけは、戦闘開始時の所属勢力と敵勢力が交戦中であることを条件とする。無所属時・非交戦国の正規軍撃破は両側とも国力変化なし。
- 神託・海図・自然探索自体には国家の依頼主がないため国力報酬を付けない。国家を代表しないイベントも対象外。

### 6.2 適用表

| 活動 | 条件 | 国力変化 |
| --- | --- | --- |
| 通常依頼 | 国家の依頼主がいて、正常に完了 | 依頼主＋1 |
| 通常の貴族依頼 | 補給・斥候・治安回復・難民受け入れ等を完了 | 依頼主＋1 |
| 戦時限定の貴族依頼 | `noble_logistics`・`noble_hunt`を完了 | 支援側＋2、指定敵側−2 |
| 前線依頼 | 防衛・襲撃・小競り合い・補給・護衛・封鎖・停戦工作の成功／完了 | 支援側＋2、指定敵側−2 |
| 依頼外の正規軍撃破 | 所属国家が敵国家と交戦中、戦闘勝利 | 所属側＋2、敵側−2 |

戦時依頼では敵側を受注時に固定し、`targetFactionId`・`enemyFactionId`など既存フィールドから明示的に対応付ける。相手を識別できない依頼から別の交戦国を推測して減らさない。

現行の前線行動は依頼を生成する方式なので、行動選択時には加算しない。正規軍の関所突破（`checkpoint_force`）は依頼外の正規軍戦闘として扱う。商人襲撃・救援など、それ以外のイベント戦闘は対象外。

戦時依頼が終戦後も既存仕様で完了可能な場合は、依頼主への通常依頼相当＋1のみとし、旧敵国は減らさない。停戦工作自身が戦争を終結させる場合は、成功直前の交戦状態で＋2／−2を決めてから既存の終戦処理へ進む。

### 6.3 重複・取り消し

- 一つの活動の成功に対して国力報酬は1回。戦闘が依頼に属する場合は通常正規軍撃破の加算を行わず、依頼完了時の加算だけを行う。
- 2戦必要な依頼は両方を終えて依頼全体が完了した時に1回。1戦目の勝利だけでは国力を増減しない。
- 前線行動が依頼を生成するだけの場合は、生成時・受注時には加算せず完了時に適用する。
- 失敗・逃走・引き分け・破棄・期限切れ・前線整理による無効化では、この活動報酬を適用しない。今回指定されていない追加の敗北ペナルティは導入しない。
- 加算済みの状態と依頼消費・戦闘結果確定を同じ保存境界で扱う。再描画、保存後の再読込、確認の二重押下で再付与しない。
- 実装時に各完了経路を一覧化する。既存の戦況加算関数へ一律に国力加算を埋め込む方法は採用しない。

## 7. 拠点喪失

- 有効な前線が攻撃側勝利となり、拠点の所有者が実際に変わった時だけ旧所有者から村40・街80を減らす。
- 防衛側勝利、既に所有者が変わった不正な前線、読み込み時の前線整理では発生させない。
- 占領側には即時の国力ボーナスを付けない。次季節からの回復量増加を利益とする。
- 拠点喪失は依頼・戦闘報酬とは別の世界変化なので、プレイヤーの依頼完了をきっかけに陥落した場合は両方を適用してよい。結果欄で理由を分ける。
- 最後の拠点を失った場合も国力値を残す。今回の機能として復国・拠点消滅・新たな滅亡処理は導入しない。

## 8. 軍資金提供

- 謁見中の貴族の所属国家について、プレイヤーが名誉家臣である場合のみ提供できる。訪問先の拠点所有者だけで判定しない。
- 謁見の行動欄に「軍資金を提供」を追加。正の整数資金を入力できる。任意額で、1,000資金単位の入力制限は設けない。
- 初期比率では1資金＝国力0.001。比率変更時も効果量を共通関数で計算し、内部最小単位未満で増分0となる提供は受け付けない。
- 提供画面に所持資金、提供額、提供前後の国力、増加量を表示する。「提供内容を確認」で入力を固定し、同じ画面の「軍資金を提供する」で確定する。対象勢力と実支払額を表示し、入力に戻る操作も用意する。
- 上限までに必要な金額を超える入力は確定不可とし、提供可能な最大額を案内する。全額を支払わせて超過分だけ消す処理は行わない。
- 国力上限時は提供不可。名誉家臣資格・謁見相手・資金・国力を確定直前にも検証する。
- 提供による日数消費、好感度、名声、支持度の追加報酬は今回は設けない。既存の賄賂等とは別の操作とする。
- 資金減算と国力加算を一括反映し、保存失敗時は両方を戻す。二重確定も防ぐ。

## 9. 表示・通知

- 勢力シートに国力の現在値／上限とゲージを表示する。海賊には国力欄を表示しない。
- 内訳は所有する街・村の数、次季節の回復予定、攻撃・防衛の件数と日次消耗を表示する。
- 回復予定は拠点由来の基礎回復量とする。実回復は回復時点の上限までであることを明記し、将来の戦闘や所有変化まで予測した確定値にはしない。
- 交戦相手別に現在の国力差による日次戦況補正を表示する。平時には不要な補正欄を出さない。
- 依頼の報酬欄に予定される国力効果を追加。完了報告・軍資金提供・拠点陥落では実増減を表示する。
- 日次の消耗・補正ごとにモーダルやトーストを発生させない。国力のための無制限な日次ログも保存しない。
- 季節回復は既存の季節報告にまとめられる場合に併記し、勢力ごとの追加ダイアログ連打を避ける。
- ヘッダに常時表示項目を追加しない。

## 10. 状態・保存・旧セーブ

状態案：`state.nationalPower = { version: 1, values: { 国家ID: 内部整数値 }, lastProcessedAbs: 絶対日 }`。

- 所有拠点数・前線件数・回復量・補正量は世界状態から導出し、別々の永続カウンターを増やさない。
- `lastProcessedAbs` で同日の回復・消耗・補正の重複を防ぐ。読み込みや画面更新自体では日数を進めない。
- 新規・旧セーブ移行時は現在日を処理済みとして設定し、過去の季節分を遡及加算しない。
- 国力のない旧セーブは3勢力とも600。既存の国力がある場合は維持し、不正値・不明ID・上限超過を正規化する。0を未設定扱いしない。
- 旧受注依頼の支援側は保存済みの勢力・貴族所属を優先する。情報が不足する場合のみ移行時の受注元拠点所有者を使って一度固定する。なお確定できない場合は国力報酬を付けず、推測の敵減少もしない。
- 初期値600への再移行、過去に完了した依頼への遡及加算、戦争終了時の再初期化を行わない。
- 保存・復元、探索の日数進行のロールバックにも国力を含める。既存の世界とstateの巻き戻し単位を維持する。

## 11. 責務分割・接続先

| ファイル案／既存ファイル | 責務 |
| --- | --- |
| nationalPowerConfig.js | 調整値・対象国家・定義の検証 |
| nationalPower.js | 初期化・正規化、精度変換、増減、回復・消耗・補正の純粋計算、提供の検証 |
| nationalPowerRules.js | 日次順序、前線集計、依頼の支援先固定、活動報酬の重複防止、戦闘開始条件の固定 |
| nationalPowerWorld.js | 実世界の交戦判定、依頼完了時の接続、実増減の結果・ログ表示 |
| nationalPowerUI.js | 勢力シートの内訳、提供画面、結果表示 |
| state.js / storage.js | 初期状態、保存、旧セーブ補完 |
| time.js / faction.js / warFronts.js | 季節・日次処理、交戦確認、前線整合性、決着と拠点喪失 |
| quests.js / questUI.js | 支援勢力の固定、種類別完了処理、予定報酬 |
| ui.js | 戦闘開始時の所属・敵・交戦状態の固定、通常戦闘結果、謁見導線 |
| panelUI.js / index.html / styles.css | 勢力シート・既存画面への組み込み |

勢力や世界を参照する接続処理と純粋計算を分け、既に大きい `faction.js`・`ui.js` に国力ロジックを集中させない。ゲーム状態の変更を表示関数から行わない。

### 完了経路の対応

- 通常依頼・補給・斥候・戦時補給：`completeQuest`。
- 通常討伐：`completeHuntBattleQuest`。治安回復・敵軍討伐：`completeNobleBattleQuest`。
- 難民受け入れ：`completeNobleRefugeeAt`。国家を代表しない難民イベントは対象外。
- 前線の戦闘・封鎖：`completeWarBattleQuest`。輸送護衛：`completeWarEscortAt`。搬入・停戦工作：`completeQuest`。各経路の成功時に `applyWarFrontScore` から国力報酬を呼び出す。
- 正規軍戦闘：`startPrepBattle` で条件を固定し、`processBattleOutcome` の勝利時に確定する。依頼・探索の戦闘には通常戦闘分を重ねない。
- 拠点喪失：`resolveFront` の所有者検証後、実際の攻撃側勝利時のみ減少させる。
- 依頼の `powerFactionId`・`powerEnemyId` は受注時または旧セーブ移行時に固定し、`powerAwarded` で重複を防ぐ。結果には `powerChanges` の実増減を使用する。

## 12. 検証条件

- 国家3勢力のみ、共通上限、0保持、旧セーブ600補完、既存値保持。
- 村・街の回復比率、季節・年境界、平時回復、開始時二重回復なし。
- 攻撃・防衛・複数前線・多国間戦争で消耗の合計と二重計上防止。
- 同日再実行、複数日進行、新規前線・当日決着の処理順。
- 国力差0・250・500・上限超過で補正が0・0.1・0.2・0.2。相手順序を反転した時の符号も確認。
- 海賊・無所属・非交戦関係には戦況補正や通常戦闘の国力報酬を適用しない。
- 国力変更で部隊能力・敵編成・通常の資金報酬が変わらない。
- 通常依頼、貴族依頼、戦時依頼、直接前線行動、正規軍戦闘の適用と非重複。
- 連戦の1戦目は国力報酬なし、最終完了のみ1回。放棄・失敗・期限切れは加算なし。
- 受注後の占領でも支援先が変わらない。和平後の依頼と停戦工作の終戦処理順。
- 拠点の実所有変更時のみ喪失。防衛成功・不整合前線除去・同じ決着の再呼び出しで重複減少しない。
- 任意の整数資金の提供、少額の表示精度、資金不足・資格喪失・満額時・超過入力・二重確定・保存失敗。
- PC・スマホで勢力シートと提供画面の表示を確認。日次通知が増えすぎない。

## 13. 試遊で確認する懸念

- 勝者が拠点を増やして回復量でも優位になる循環は残る。まず攻撃消耗と戦況補正上限で抑え、不足する場合は数値を調整する。
- 暫定値では拠点の多い国家が上限に張り付きやすい可能性がある。数季節の前線件数・回復・実消耗・陥落数を比較する。
- 軍資金提供は非効率な余剰資金の用途とする。大量資金だけで即座に戦争が決着しないことを確認する。
- 現行の前線判定は期限時のスコアの正負で決まるため、小さな補正も拮抗した前線には効く。補正の上限だけでなく、逆転に必要な前線行動回数も確認する。

上記の端数・日次順序・和平後の扱いは、追加質問を増やさず実装を進められるよう本設計で採用する処理方針。国力の影響範囲を広げる新要素は含めない。
