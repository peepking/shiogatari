# 探索・海図・船の艤装 追加設計書

作成日: 2026-09-11  
状態: 第4回まで実装済み。探索・海図・艤装をゲーム本体へ接続済み。今後はプレイによる数値調整を行う。
更新: ユーザーの実装開始指示により着手。R9の調整案は第1回で初期値として採用し、設定値として管理する。

## 実装進捗

| 回 | 範囲 | 状態 |
| --- | --- | --- |
| 1 | 設定定義、状態初期化、旧セーブ補完、保存往復、艤装効果の純粋計算 | 実装済み |
| 2 | 自然探索の初期配置・日次出現・消滅、地図表示、回収・戦闘と途中再開 | 実装済み |
| 3 | 海図の割当・予約・噂・報酬・依頼欄表示 | 実装済み |
| 4 | 艤装の購入・保管・交換・効果比較、兵站・戦闘への適用、衛生兵修正 | 実装済み |

配置先: 設定は `expansionConfig.js`、状態補完は `expansionState.js`、効果集計は `outfitting.js`。保存領域は `state.expansion`（`version: 1`）。自然探索の抽選・消費・保存検証は `exploration.js`、画面と日次・戦闘への接続は `explorationUI.js`、アイコンは `mapArt.js`。地図準備後に初期地点を一度だけ生成する。海図の予約・復元・報酬計算は `charts.js`、依頼と入場への接続は `chartWorld.js`、表示と探索は `chartUI.js`。艤装画面は `outfittingUI.js`。艤装効果を上限・消費・戦闘へ適用済み。

## 1. この文書の役割

漂流物・難破船・戦場跡の探索、海図の断片と発見、船の艤装を追加するための設計書。ユーザー指定の数値を保持し、未回答の内容を確定事項として扱わない。

- **確定**: ユーザーが指定・承認した内容。
- **提案**: 既存処理との整合性のための推奨案。実装前に採否を決める。
- **未決**: 数値や挙動の指定が不足している項目。
- 本書は実装時の参照資料として更新し、各回の実装済み範囲を冒頭に記録する。
- 実装後、確定した遊び方を `README.md`、実装構造と計算式を `design.md` に反映する。未実装案は転記しない。
- 配達距離・目的地強調、部隊／物資詳細の維持費予告、昇級内訳は先行改善として作業ツリーに実装済み。今回の新要素とは別に検証・文書反映する。
- 情報購入イベントの出現条件修正はこの文書の実装範囲に含めず、別件として扱う。

## 2. 既存仕様と変更しない前提

- 地図は50×50の正方形マス、拡大表示は9×9。移動は上下左右1マスで1日。
- 1季節30日、1年120日。探索地点の寿命・噂履歴には絶対日を使う。
- 現在地は白枠、選択中のマスは黄色枠。新要素でも色の意味を変えない。
- 海上の現在地は船、陸上は枠なし部隊アイコン。街・村では建物中央、部隊左下。
- 神託は選択制にしない。調達依頼の重複、市場の季節更新も維持する。
- 戦闘は1tick＝ゲーム内1秒、最大60tick。表示速度1／2／4倍は現実の進行速度だけを変更する。
- 通常攻撃の現行式は `max(1, round(有効ATK * 100 / (100 + 有効DEF)))`。有効DEFには地形補正が含まれる。
- 部隊数ではなく兵員を保有し、戦闘時に部隊を編成する。固定の部隊名・戦歴は追加しない。
- 物資上限は `60 + 45 * ships`、兵員上限は `30 + 15 * ships`。
- 現在の食料消費日は10・30日、必要量は全兵員数÷4の切り捨て。維持費は季節1日に兵種別費用を合計する。
- 現行の `design.md` には食料消費日など古い記述が残るため、実装時は実コードを確認する。

## 3. 漂流物・難破船・戦場跡

### 3.1 確定仕様

| 項目 | 内容 |
| --- | --- |
| 配置数 | 漂流物は最大5個、難破船は最大2個を目安にする |
| 出現 | 毎日確率で出現。最大数に達している場合は出現しない |
| 寿命 | 出現から120日で自然消滅 |
| 漂流物／戦場跡報酬 | 資金500前後＋高級品2種類・合計3個前後。名声等でスケールしない |
| 難破船報酬 | 資金・高級品は上記の倍。加えて部隊員と確率で船。報酬はスケールしない |
| 出現頻度 | 漂流物／戦場跡は2季節に3件程度、難破船は半年に1件程度 |
| 初期配置 | 漂流物・戦場跡・難破船を各1件 |
| 戦闘確率 | 0%・25%・50%・75%・100%の5段階。段階別にダイアログの案内を変える |
| 敗北・逃走・引き分け | 報酬なし。探索地点も消滅 |
| 陸上の名称 | 漂流物に相当する地点を「戦場跡」と表示 |
| 探索 | 1日消費。確率で賞金首相当の敵と戦闘 |
| 表示 | 種別ごとに地図アイコンを追加 |

### 3.2 配置・寿命の提案

- 漂流物と戦場跡は共通枠として合計5個。難破船は別枠2個。上限値は定数化する。
- `sea`／`shoal` に漂流物と難破船、それ以外の非拠点地形に戦場跡を置く。
- 街・村、既存探索地点、海図の目標地点と重ねない。依頼の固定戦闘地点とも可能な限り重複を避ける。
- 毎日、期限切れ地点の除去→種別ごとの出現判定→配置の順。1日につき共通枠・難破船枠それぞれ最大1件を生成する。
- `expiresAbs = spawnedAbs + 120` とし、`today >= expiresAbs` で消滅。候補地がなければその日の生成を見送り、無制限な再抽選をしない。
- 探索開始済みの地点は予約状態にし、日付進行や戦闘中に自然消滅させない。
- 初期配置は各種1件。旧セーブへの初回導入でも一度だけ生成し、再読み込みで補充しない。
- 出現頻度は合算で確定。1年120日のため2季節・半年はいずれも60日。漂流物／戦場跡の共通枠は1日5%（3/60）、難破船は1日1/60（約1.67%）とする。上限や候補地不足で見送る分を除く期待値であり、期間内の出現数を保証しない。
- 海陸の抽選比率は調整項目。漂流物／戦場跡を各50%で選んでから適合地形に配置する案。

### 3.3 探索の進行案

1. 現在地が探索地点なら行動欄に「探索する」を表示する。
2. ダイアログで名称、残存日数、所要1日、戦闘の可能性を表示する。
3. 確定時に地点を予約し、結果・敵・報酬候補を一度だけ抽選する。
4. 1日進行させる。維持費・食料消費は通常どおり適用する。
5. 敵がいなければ探索報酬を付与。敵がいれば既存の戦闘準備へ移る。
6. 戦闘勝利後に探索報酬を付与し、地点を消去する。
7. 結果ダイアログを閉じて通常画面へ戻り、完了状態を保存する。

敵が出た場合の探索報酬は勝利まで保留する。敗北・逃走・引き分けは探索報酬なしで地点を消去する（確定）。通常戦闘の報酬・損耗・昇級などは既存どおり処理し、勝利時には通常戦闘報酬と探索報酬を併給する（確定）。スケールしないのは探索報酬のみであり、通常戦闘の報酬計算は変更しない。結果画面では両者を区別する。

探索中に日次の別イベントが発生する場合はキューへ積み、探索の戦闘と二重に開始しない。延期できない既存イベントの優先順は実装時に確認する。

### 3.4 報酬・上限超過

- 資金・高級品は名声・敵人数・季節・経過日数に依存しない。敵の強さを報酬額の計算に流用しない。
- 具体値案: 漂流物／戦場跡は資金450～550、高級品は重複なしで2種類を選び合計2～4個を両種に1個以上配分。難破船は資金900～1100、高級品合計4～8個。いずれも基準値・幅の整数一様抽選を想定し、配分とともに未確定の調整案として扱う。
- 難破船の救助兵員はLv1・合計3～5人、船は20%で1隻（確定）。兵種の抽選表は調整項目。救助報酬も名声や敵人数に依存させない。
- 戦闘確率は地点生成時に固定し、5段階を各20%で抽選する（確定）。探索ダイアログを開き直しても変えない。

| 戦闘確率 | 案内文の提案 |
| --- | --- |
| 0% | 安全に探索できそうです（戦闘確率0%） |
| 25% | 敵が潜んでいる可能性があります（戦闘確率25%） |
| 50% | 敵と遭遇する可能性は半々です（戦闘確率50%） |
| 75% | 敵がいる可能性が高い場所です（戦闘確率75%） |
| 100% | 探索すると必ず戦闘になります（戦闘確率100%） |

- 0%は必ず非戦闘、100%は必ず戦闘。地点自体の危険と日次の別イベントを区別する。
- 難破船の部隊員を既存戦闘の拿捕分と混同しない。結果では「救助した兵員」など獲得元を分ける。
- 提案: 船を先に加算して上限を再計算し、その後に物資・兵員を加算する。
- 上限超過時に報酬を黙って切り捨てない。超過量と破棄・解雇への導線を表示し、既存の移動制限を適用する。
- 探索用の持ち帰り選択画面を新設する案は未承認。初期実装では既存の整理操作の再利用を推奨する。

## 4. 海図の断片と発見

### 4.1 確定仕様

- 3枚で完成する海図と5枚で完成する海図があり、各サイズにつき最大1件、合計最大2件を同時進行する。
- 戦闘報酬では超低確率、依頼報酬では低確率で、資金の代わりに断片を獲得する。依頼は受注前に断片報酬だと分かるようにする。
- 初期調整値は戦闘勝利時0.5%、資金報酬のある依頼の生成時3%（確定）。ユーザーのプレイで調整できるよう定数化する。
- 村・街に入るとランダムで断片の噂が発生し、拠点からおよそ10～15マス離れた地点に取得地点を配置する。
- 同じ拠点で足踏みして噂を繰り返せないようにする。噂から得られる断片は同じ海図につき最大1枚。
- 断片獲得時にダイアログを表示。最初の断片では何が記されているかも知らせる。
- 進捗は依頼欄の下側に専用領域として常時表示し、海図の内容をいつでも確認できるようにする。
- 海図は次の3種類。完成報酬には少量の名声も付く。

| 海図 | 3枚の報酬 | 5枚の報酬 |
| --- | --- | --- |
| 古い祭壇 | 信仰90～110（100±10%） | 信仰180～220（200±10%） |
| 隠れた入り江 | 高級品5種類へランダム配分・合計100＋船2隻 | 高級品5種類へランダム配分・合計200＋船4隻 |
| 財宝の隠し場所 | 資金25000 | 資金50000 |

共通の名声は3枚版が9～11、5枚版が18～22。R9の初期調整値として実装する。

### 4.2 進行モデル（提案承認済み、残る未決を明記）

`collecting（収集中）→ located（完成・場所判明）→ exploring（現地探索中）→ completed（報酬獲得済み）`

- 初回の断片で海図ID、種類、必要枚数を確定する。同じ種類の3枚版と5枚版の並行は許容する。予約が先に発生した場合の内部IDは予約時に確定し、初回獲得時に内容を通知する。
- 海図は通常依頼と同じスクロール領域の末尾に配置し、依頼一覧の折りたたみに追従する。獲得前は信仰・高級品・船・資金・名声のうち該当する種類だけを表示し、数量・範囲・高級品の種類数は明かさない。完成時に最終地点を明かし、現地へ赴いて報酬を得るまで枠を保持する。獲得結果では実際の数量を表示する。
- 完成地点にも共通の探索地点モデルを使う。ただし120日消滅やランダム戦闘を自動適用しない。
- 噂地点・完成地点の寿命は無期限（確定）。完成地点に到着後、1日使って探索し、追加戦闘なしで報酬を獲得する（確定）。通常の日次維持費・消費は適用する。
- 依頼欄の海図領域は上部へ割り込ませない。スクロールで通常依頼を隠す固定オーバーレイにはしない。見出し・枚数・種類を簡潔に常設し、説明は展開表示する。
- 噂だけが先に発生した場合の表示は「噂の手掛かり」とし、断片を取得した扱いにはしない。

### 4.3 噂の連続取得対策（提案承認済み）

- 追加調整: 噂は季節初回入場5%へ半減。自然探索の断片は漂流物・戦場跡5%、難破船10%。探索開始時に当落を保存し、探索成功時のみ付与する。通常の戦闘断片抽選も維持する。
- 海図売り: 移動イベントの共通クールダウン・優先順に従い、災いの兆しの直後に1%で抽選。1000資金で本物25%。古文書商: 街ごとの季節初回入場10%、2000資金で本物50%。村では出ない。
- それぞれの購入元から同じ海図に本物1枚まで。偽物では取得上限を消費しない。候補と当落をイベントに保存し、購入時は予約・取得上限・資金を再検証する。未予約枠がなければ販売イベントは出さず、購入前に条件が変わった場合も資金を消費しない。販売は予約枠を新設せず、既存の依頼・噂の予約を優先する。

- 入場判定は実際の村・街への入場時だけ。モーダル開閉や再描画では抽選しない。
- 同じ拠点では季節に1回だけ抽選する。不発でも抽選済みと記録する。
- 発生率は季節初回入場で5%（確定）。進行中の海図を優先し、新規なら空いているサイズ枠と3種類から均等抽選する。詳細はR3。
- 同じ海図に未回収の噂地点がある間は、2つ目を配置しない。
- 噂の断片は現地回収を必須とし、他経路の断片で予約枠を埋めない。カードは取得済み・依頼予約・噂予約を区別する（確定、R1）。
- 海図単位で `rumorFragmentClaimed` を記録し、1枚取得後はその海図の噂を生成しない。
- 調査地点までの距離は上下左右移動の最短距離で10～15マス。候補地がない場合は生成を見送る。
- 噂地点での断片回収は1日消費・追加戦闘なし（確定）。通常の日次維持費・食料消費は適用する。
- 3枚／5枚の両枠が進行中なら、その中の噂未取得・収集中の海図を対象にする。
- 両枠が完成待ちの場合、進まない断片の噂を発生させない。
- 断片未所持でも噂が出て、海図集めを開始できる（確定）。この場合は噂生成時に海図の内部IDと進行枠・断片1枚分を予約し、取得までは「噂の手掛かり」と表示する。噂地点に到着して獲得した時点で初回断片の説明を表示する。
- 拠点をまたぐ全体クールダウンは追加せず、季節ごとの拠点判定と海図単位の1枚制限を基本とする案。

### 4.4 断片報酬を固定する設計（予約方式を承認）

- 資金を置き換える判定は一度だけ行う。依頼は候補生成時、通常の戦闘は勝利報酬の確定時に判定する。
- 依頼の表示・受注・達成・ロードで再抽選しない。信仰・名声・好感度など、資金以外は置き換えない。
- 未受注の依頼候補は進行枠を占有しない。受注時に割当先を再検証し、割当不能なら理由を示して受注不可とする。予告した断片を黙って資金に戻さない。
- 受注済みの断片報酬は海図IDに紐付けて予約する。取得済み枚数＋予約済み枚数が必要枚数を超える候補は受注できない。
- 噂の生成・回収も予約数を考慮し、完成直前に余剰断片を生まないようにする。
- 依頼失敗時は予約を解放する。枠に余裕がなければ通常戦闘は資金報酬を維持し、報酬なしにしない。
- 上記予約方式を採用。任意の海図へ使える未割当断片は追加しない。
- 抽選対象は資金報酬のある依頼。資金報酬のない依頼は対象外。予告された断片報酬がある討伐依頼では、その戦闘の追加断片抽選を省く（確定）。
- 複数戦から成る依頼でも、断片報酬を予告している依頼に紐づく各戦闘では追加抽選を省く解釈を推奨する。最終的な適用範囲は監査項目R7を参照。

### 4.5 報酬詳細と残る調整

- 信仰の変動幅は基準値の±10%。範囲内の整数を一様抽選する案。
- 高級品は5種類で合計50個、5枚版は100個。配分案は各種1個を先に割り当て、残りを1個ずつ5種均等に抽選して加算する。必ず全5種が含まれ、合計も一致する。
- 名声の基準値は10／20。変動幅は9～11／18～22（±10%）を提案する。
- 完成地点は海図割当時の現在地から最短距離10～20マスで内部固定し、完成時に公開する（確定）。入り江は海／浅瀬、祭壇・財宝は非拠点陸地。詳細はR2。
- 断片の出現率と3枚／5枚版の抽選比率。すべて設定表に集約する。

## 5. 船の艤装

### 5.1 確定仕様

- 艤装は街でのみ行える。初期装備枠は1枠。
- 最大5枠。2枠目は15000資金、3枠目は30000資金、4枠目は50000資金、5枠目は100000資金で順番に拡張する。
- 同じ設備は同時に複数装備できない。
- 購入済みの設備は保管し、付け替えできる。
- 装備変更前に変更後の上限と超過量を表示する。超過分は既存の売却・破棄・解雇で整理できる。
- 艤装効果は陸戦も対象。兵站・救命・見張り効果も海陸で無効化しない。

| 分類 | 名称 | 価格 | 効果 |
| --- | --- | ---: | --- |
| 攻撃 | モリ投擲 | 5000 | 5tickごとに敵1部隊へ威力30の支援射撃。DEF軽減あり、射程無限 |
| 攻撃 | バリスタ | 15000 | 10tickごとに敵1部隊へ威力100の支援射撃。DEF軽減あり、射程無限 |
| 攻撃 | ファイヤバリスタ | 15000 | 15tickごとに敵1部隊へ威力150の支援射撃。DEF軽減あり、射程無限、継続燃焼なし |
| 攻撃 | ブドウ弾バリスタ | 15000 | 6tickごとに生存敵全部隊へ各威力30。対象ごとにDEF軽減、射程無限 |
| 攻撃 | 火炎ブドウ弾バリスタ | 15000 | 12tickごとに生存敵全部隊へ各威力60。対象ごとにDEF軽減、射程無限、継続燃焼なし |
| 攻撃 | 砲撃支援 | 25000 | 20tickごとに敵1部隊へ威力250の支援射撃。DEF軽減あり、射程無限 |
| バフ | 鉄の被膜 | 15000 | 全兵員DEF＋5% |
| バフ | 舷の丸盾 | 15000 | 近接兵員DEF＋10% |
| バフ | 天蓋 | 15000 | 遠隔兵員DEF＋10% |
| バフ | 火鉢 | 15000 | 全兵員ATK＋5% |
| バフ | 衝角 | 15000 | 近接兵員ATK＋10% |
| バフ | 矢の箱 | 15000 | 遠隔兵員ATK＋10%（固定値＋10ではない） |
| 兵站 | 貨物テント | 5000 | 物資上限＋20% |
| 兵站 | 胸壁 | 5000 | 兵員上限＋20% |
| 兵站 | 拡張船倉 | 5000 | 物資・兵員上限＋10% |
| 兵站 | 甲板のテント | 5000 | 食料消費－10% |
| 兵站 | 嵐対策用の覆い | 5000 | 兵員維持費－10% |
| 兵站 | 船大工の設備 | 5000 | 船維持費－10%。フリュートの軽減と加算、部隊維持費とは別計算 |
| 兵站 | 救命設備 | 10000 | 衛生兵5人分を加算。実兵員と合わせ効果は最大10人分 |
| 兵站 | 見張り台 | 10000 | 斥候5人分を加算。実兵員と合わせ効果は最大10人分 |

名称注記: 「難波船」「貨物物テント」は表記上「難破船」「貨物テント」に統一する案。設備効果は変更しない。

### 5.2 攻撃の設計と確認事項

- 砲撃支援は威力250・20tick間隔・25000資金。1戦1発には制限しない。ガレアスは発射間隔を変えず、威力を最大20%増加させる。
- 初回発動はモリ5tick、バリスタ10tick、ファイヤバリスタ15tick、砲撃20tick。戦闘開始直後の発射はしない。船種補正は `shipyard-design.md` に従う。
- 一時停止中は進まず、速度変更で発射回数は変えない。
- 同時発射は装備枠順で処理し、各発射時に生存敵から再選択する。既に撃破された相手に重ねて発射しない。
- ターゲットは生存敵から均等抽選する。部隊の攻撃方針とは独立する（標的選択案の承認）。
- 艤装射撃後にも生存判定を行い、撃破・勝利・戦果集計を通常攻撃と共用する。砲撃は戦後の拿捕や討伐達成を迂回しない。
- 各tickで「通常行動→支援射撃→終了判定→60tickの時間切れ判定」とする案。ただし通常行動で勝敗が決まっていれば支援射撃を行わない。
- 砲撃の発動候補は20・40・60tick。戦闘がそこまで続いている場合のみ発射する。
- ブドウ弾2種は `attack.allEnemies` により、発射時の生存敵それぞれを1回ずつ攻撃する。初弾は6／12tick。単体射撃の均等抽選とは分け、味方と撃破済みの敵は対象外。枠順に発動し、先行する射撃で全滅したら以降は発射しない。水色寄りの青／赤い炎色で全対象への射撃を描画する。

**防御計算は全支援射撃で現行式を共用する。** DEF46ならモリ（威力30）は21、バリスタ（100）は68、砲撃支援（250）は171ダメージとなる。ガレアスによる威力補正はDEF軽減前に適用する。

### 5.3 バフ・兵站計算（提案・消費の丸めとも承認済み）

- 設備は船団共通とし、船数で効果や装備枠を増やさない。初期の聖船でも使用可能。
- 近接／遠隔の区分は既存兵種の射程定義を利用し、表示名で比較しない。
- 同じ能力の割合効果は加算する。鉄の被膜＋舷の丸盾なら近接DEF＋15%、遠隔DEF＋5%。
- 戦闘開始時に装備の有効効果を固定する。人数・レベル補正後の値へ艤装倍率を掛け、地形補正と重複して二度適用しない。
- バフの丸め位置は既存能力計算と統一し、計算用値を再描画のたびに増やさない。
- 上限は `floor(既存上限 * (1 + 増加率合計))` とする。貨物テント＋拡張船倉なら物資上限＋30%。
- 維持費は全兵員の維持費を合計してから10%軽減し、最後に切り捨てる（確定）。兵種・レベル・部隊ごとには丸めない。装備時は `floor(維持費合計 * 90 / 100)`。
- 食料も既存の必要量 `floor(全兵員数 / 4)` を算出してから10%軽減し、最後に切り捨てる（確定）。装備時は `floor(既存必要量 * 90 / 100)`。少人数時の端数による実質軽減率の増加は許容する。
- 消費処理と維持費予告は同じ計算関数を参照する。艤装変更直後の予告も更新する。
- 救命設備・見張り台は各兵種5人分を加算する（確定）。実兵員・積載・維持費・食料消費には加算しない。
- 衛生兵は待機中も含む保有人数を戦闘開始時に記録して集計する。現行の出撃兵のみを数える処理は不具合として実装時に修正する（承認済み、R4）。
- 効果人数はそれぞれ `min(10, 実人数 + 設備による5人分)`（確定）。既存の斥候処理に人数上限が明示されていない場合も、この仕様への統一箇所として検証する。
- 攻撃系・バフ系・救命／見張り設備は陸戦にも適用する（確定）。
- バランス所見: 救命設備単独で損耗率50%→25%、衛生兵5人と併用で0%。見張り台単独で逃走率は約94.6%、斥候5人と併用で約99.6%。現時点では追加強化せず、装備枠の競合を含め実戦確認する。これらは現行の損耗・逃走式に基づく試算であり、戦闘勝率の保証ではない。

### 5.4 艤装画面の提案

- 街の行動欄の「船の艤装」で中央の地図カードを艤装画面に切り替える。「地図に戻る」で終了し、街を離れた場合も閉じる。購入・変更の最終確認だけ小さなダイアログにする。
- ヘッダの「艤装変更」を初期表示とし、「詳細確認」で現在の全数値と装備中の艤装・効果を表示する。変更画面の比較は変化する数値のみ表示する。
- 地図カードと幅を統一し、カテゴリ変更で下部の位置・高さを変えない。一覧と比較欄は独立してスクロールし、狭いカードでは上下に並べる。同じカテゴリで設備を選んだ際は一覧のスクロール位置を保つ。
- 支援射撃はモリが水色、バリスタが紫、ファイヤバリスタが赤い炎色、砲撃が橙色。砲撃、ファイヤバリスタ、バリスタ、モリの順に線幅・発光・着弾を大きくする。
- 上部に装備枠と拡張費用、下部に攻撃・バフ・兵站の設備一覧を表示する。
- 各設備に価格・効果・未購入／保管中／装備中を表示する。
- 購入・装備前の実効比較を表示する（確定）。維持費120→108、物資上限105→126、衛生兵効果の増分0など、現在の兵員・設備に基づく差を示す。
- 枠が満杯の場合は交換対象を選んでから比較する。既存装備に単純加算した仮値を購入効果として表示しない。ATK／DEFは対象兵種と倍率、支援射撃は威力・間隔・標的方式を示し、敵DEF不明時に実ダメージを断定しない。
- 未購入設備は購入、保管中は装備、装備中は取り外しを選べる。購入だけで自動装備しない案。
- 拡張は2枠目→3枠目の順に購入。資金不足や二重購入は不可。
- 付け替え費用は無料、売却は初期実装の対象外とする案。
- 変更確認で物資・兵員の現所持数、変更後上限、超過量を表示する。超過しても確認後の取り外しは可能とする案。
- 戦闘中・街の外では購入・装備変更不可。閉じるだけでは未確定の変更を反映しない。
- 戦闘画面には有効設備をまとめて表示し、支援射撃は発射元の船側から敵へのエフェクトで区別する。部隊詳細のATK／DEFも効果込みにする。

## 6. 共通構造・保存設計案

### 6.1 責務分割

| 追加モジュール案 | 責務 |
| --- | --- |
| `exploration.js` | 探索地点の生成・期限・予約・進行 |
| `charts.js` | 海図の進捗、断片予約・取得、噂履歴、完成地点 |
| `outfitting.js` | 設備定義、購入・装備、効果集計 |
| `explorationUI.js` | 探索・海図の操作、結果・進捗表示 |
| `outfittingUI.js` | 中央カードの艤装画面と変更確認 |

- 各機能の数値設定をまとめ、出現率や価格をUI内に重複記述しない。
- `time.js` は日次更新を呼ぶだけ、`actions.js` は入場・探索開始の入口に留める。
- `battle.js` は艤装の戦闘効果を適用し、`ui.js` は既存戦後処理から探索・断片の結果確定を呼ぶ。
- `quests.js` は報酬の割当・予約、`questUI.js` は断片報酬と海図進捗の表示へ接続する。
- `mapArt.js` に地点の描画を追加し、`map.js` から地点データを渡す。表示処理で乱数を消費しない。
- `supplies.js`・`troops.js` の上限計算、`upkeep.js` の予告と実消費を同じ艤装集計に接続する。

### 6.2 状態の概念モデル

| 状態 | 主な項目 |
| --- | --- |
| 探索地点 | 一意ID、内部種別、座標、出現絶対日、消滅絶対日（無期限はnull）、固定危険度、進行状態、関連海図ID |
| 探索処理 | 一意ID、対象地点ID、開始日、日数適用済み、固定済み敵・報酬、付与済み状態 |
| 海図 | 一意ID、種類、必要枚数、取得済み枚数、予約数、噂取得済み、最終地点、進行状態 |
| 噂履歴 | 拠点ID→最終抽選季節、海図ID→未回収地点ID |
| 艤装 | 開放枠数、購入済み設備ID集合、枠ごとの設備ID |
| 依頼報酬 | 資金／断片の内部種別、元の資金額、予約海図ID、付与済み状態 |

内部IDに日本語名を使わない。描画用名や説明は定義表から取得する。モジュール名・状態キーの最終形は既存規約に合わせて実装時に確定する。

### 6.3 保存・復元と二重獲得防止

- 初期化と旧セーブ補完で、初期配置済みフラグを使って自然探索地点を各種1件だけ生成する。海図なし・購入設備なし・1枠を設定する。既存の探索状態を復元できた場合は初期配置を追加しない。
- 既存の依頼報酬は復元時に断片へ再抽選しない。旧セーブの資金報酬を維持する。
- 報酬付与と消費済み地点／予約解放を同じ完了処理で更新し、保存を要求する。
- 獲得ダイアログの閉じる・再表示は報酬付与の入口にしない。
- 既存セーブは戦闘準備・戦闘中に保存しないため、探索確定前のチェックポイント方針が必要。提案は探索開始時の固定済み結果と予約を通常状態で保存し、ロード時にその探索へ復帰する方式。
- 日数消費の適用済みフラグを保存し、再開で追加の1日や別の敵を発生させない。戦闘自体の途中保存は追加しない。
- 保存失敗時に成功表示だけで進めず、従来の失敗通知を使う。ブラウザ保存を任意に巻き戻す行為まで防止する設計にはしない。
- 不明設備ID、重複装備、枠数超過、範囲外座標、壊れた関連IDを復元時に検査する。
- 完了済み地点は削除し、無制限な完了履歴を持たない。噂履歴は拠点数に比例する最終判定だけを残す。ログは既存の200件上限を維持する。

## 7. 地図・ダイアログの見た目

- 漂流物: 木箱と浮材。難破船: 壊れた船体と傾いたマスト。戦場跡: 折れた旗と盾。既存の地形・船に合う輪郭と色調で描く。
- 海図の噂: 断片を示す小さな紙片。完成地点: 祭壇／入り江／宝箱を区別する。
- 丸いバッジ枠や、新たな黄色の現在地枠は追加しない。
- 全体表示では簡略記号、拡大表示では詳しい図柄。現在地と重なったら地点中央・部隊左下を基本案とし、海上の船との重なりも確認する。
- 配達先強調の導線を海図の「場所を確認」に共用する。
- 結果ダイアログの資金・物資・信仰・船・兵員・名声は既存の資源アイコンを使う。ログは文字のみ。
- ボタンは通常幅で横書き。海図2件と多数の依頼がある場合、狭い画面、長い設備説明を確認する。

## 8. 実装順と検証

1. 未決事項の確定、数値設定表、状態の旧セーブ補完を作成。
2. 共通探索地点と日次生成・消滅、予約・報酬の一度だけの確定を実装。
3. 漂流物・難破船・戦場跡の地図・探索・戦闘連携を実装。
4. 海図の進捗・噂・断片報酬・完成地点を実装。
5. 艤装の購入・保管・枠拡張と兵站効果を実装。
6. バフ・支援射撃・戦闘結果への反映を実装。
7. 通しプレイで収益・遭遇頻度・装備性能を検証し、確定仕様を既存文書へ反映。

必須検証:

- 配置上限、候補地ゼロ、120日境界、年越し、複数日進行、予約地点の期限保護。
- 探索キャンセル、日次イベント競合、勝利・敗北・逃走・引き分け、二重クリック、ロードと報酬の二重付与。
- 船を含む報酬の上限再計算、超過物資・兵員の整理、上限を超えたまま移動できないこと。
- 噂の再入場・季節更新・拠点横断・未回収地点・同一海図1枚制限。
- 3枚／5枚同時進行、予約中に別経路で取得、依頼失敗による予約解放、両枠が完成待ちのケース。
- 断片報酬の受注前表示と実際の獲得一致、旧依頼の資金報酬維持。
- 艤装の二重購入、重複装備、資金不足、保管と付け替え、拡張順、取り外し時の超過予告。
- バフの累積加算防止、近接／遠隔区分、地形との順序、維持費予告と実消費の一致。
- 救命設備なしでも待機衛生兵が有効であること、設備と合算して10人上限になること、戦後に加入した衛生兵が遡って効果を与えないこと。
- 支援射撃の5／10／20tick、同時発射、撃破済み標的、一時停止・速度変更、60tick境界、戦果集計。
- 旧セーブ・途中再開・破損フィールド・保存容量不足、ログ上限。

## 9. 残る調整項目

| 優先 | 項目 |
| --- | --- |
| 確定 | R1～R5は承認済み。再確認不要 |
| プレイ後 | 漂流物／戦場跡の海陸比率、難破船の救助兵種の抽選表（R9の初期値を実装済み） |
| プレイ後 | 固定探索報酬、名声の変動幅、高級品の配分（R9の初期値を実装済み） |

未決項目を決定したら本書を更新し、実装中の調整も理由とともに残す。実装完了後は本書を経緯資料として保持し、日常の説明・保守は `README.md` と `design.md` を参照できる状態にする。

## 10. 採否の記録

- 海図室: 効果が弱いため今回は不採用。
- 回収クレーン・信号旗: 世界観に対して近代的すぎるため不採用。実装候補に残さない。
- 購入・装備前の実効比較: 採用。5.4に統合済み。
- 新設備は増やさず、確定済み設備を実装対象とする。

## 11. 実装前監査：質問・懸念と解決案

本節は実コードとの照合で見つかった問題を記録する。ユーザーの判断が必要な事項と、実装上の対処を区別する。以下の推奨値・ルールは、確定済み仕様を上書きしない。

### R1. 無期限の噂による断片枠の固定（確定）

噂の1枚を予約したままなら、残りを集めてもその地点へ行くまで完成できない。これは現行の予約方式から生じる挙動。無期限の地点自体を消す必要はないが、噂に向かわず戦闘・依頼だけで同じ海図を完成させることはできなくなる。

噂の1枚は確保済みの取得経路として維持し、カードに「取得1／3・依頼予約1・噂予約1」のように表示する（承認済み）。予約分を取得済み進捗へ混ぜない。現地回収を必須とし、破棄・予約解除による別経路への置換や再抽選は追加しない。

### R2. 海図完成地点の距離と公開時期（確定）

噂地点は発生拠点から10～15マス。完成地点は海図割当時の現在地から10～20マスで確定した。両者の基準地点と距離条件を混同しない。

確定: 最初の海図割当時の現在地を基準に、最短距離10～20マスから完成地点を選び内部的に予約し、全断片取得時に公開する。入り江は海／浅瀬、祭壇・財宝は非拠点陸地。後から完成位置を都合よく変えるための再抽選を防ぐ。候補がない場合は距離条件だけを段階的に緩和する。

### R3. 噂の発生率と海図の割当（確定）

断片の戦闘0.5%・依頼3%に加え、噂は対象拠点への季節初回入場で5%と確定した。

確定: 対象拠点への季節初回入場で5%。既存の収集中海図へ優先割当し、2件とも対象なら均等抽選。新規作成時は空いている3枚／5枚枠から均等抽選し、種類は3種均等。予約だけの海図も既存扱い。条件を満たさないときは噂を生成しない。

### R4. 待機中の衛生兵の扱い（修正承認済み）

READMEは待機中も有効としているが、現行 `ui.js` の `calcLosses` は戦闘結果の味方ユニットから衛生兵を合計する。出撃しなかった衛生兵を含める設計になっていない。

修正承認済み: 説明どおり待機中も含む保有人数を戦闘開始時に記録し、救命設備の5人分を加えて10人上限を適用する。戦後の救助・拿捕で増えた衛生兵はその戦闘の軽減に遡って使わない。斥候も同じ集計責務へまとめ、設備分を実兵員へ追加しない。既存処理の誤りとして実装時に修正する。装備なしでも待機衛生兵が有効なことを回帰テストに含める。

### R5. 探索で遭遇した敵からの逃走（確定）

自然探索は賞金首相当の編成の強さだけを継承し、戦闘開始前の逃走を許可する（確定）。既存の討伐依頼の逃走可否は変更しない。

確定: 自然探索では遭遇戦の準備画面を使い、戦闘開始前の逃走を許可する。成功時は探索地点を消去、報酬なし。失敗時は再抽選・再逃走の連打で安全化させず戦闘へ進む。降伏も地点消去・探索報酬なしとし、通常の降伏ペナルティを適用する。戦闘開始後の任意撤退ボタンは追加しない。

### R6. 資金から断片への置換経路（実装上の注意）

`acceptQuest` は調達・配達の資金報酬を受注時に再計算する。候補生成時に単に資金を0にしても受注時に復活するため、金額とは独立した報酬種別が必要。

- 報酬種別を受注時も維持し、元の資金額は表示・検証用に別保持する。
- 断片の予約検証を候補リストからの削除・配達物資の加算より先に行う。失敗した受注で物資だけ増えないようにする。
- 表示済み候補の海図サイズ・種類を受注時に変えない。対象海図が完了済みなどで割当不可なら受注不可の理由を表示する。
- 予約は件数カウンターだけでなく依頼ID／噂地点IDの参照から復元・検証できるようにする。依頼失敗・期限切れ・候補更新で不正な予約が残らないようにする。
- 断片未取得・予約なしになった仮の海図は枠を解放する。噂地点が残る海図は解放しない。

### R7. 複数の報酬源と抽選の重複（実装方針案）

- 戦闘勝利と依頼達成は別の報酬源。断片報酬を予告した依頼に関連する戦闘では0.5%抽選を行わない。
- 連戦型依頼も、その依頼IDが付いた各戦闘で同じ抑止規則を使う。
- 通常の資金報酬の依頼に関連する戦闘は、戦闘側0.5%の対象。成功しても依頼側の資金報酬は維持する。
- 自然探索戦で戦闘側の断片抽選が成功した場合は通常戦闘の資金分だけを置き換える。固定の探索資金は維持する。
- 報酬ダイアログには戦闘報酬・依頼報酬・探索報酬を識別できる形で表示する。

### R8. 日次イベント・保存の中断点（実装上の注意）

- 「追加戦闘なし」の噂回収・完成地点探索でも、1日進行の既存処理は別の遭遇イベントを発生させる可能性がある。探索由来の敵は生成せず、日次イベントは探索完了後に処理する案。説明も探索自体の戦闘有無を示す。
- 探索開始前に、地点予約・固定結果・日数適用前の記録を保存する。1日適用後に日数適用済み状態と日次イベントキューをまとめて保存し、その後戦闘準備に入る。ロード時は最後に完了保存された段階から再開する。
- 戦闘中は従来どおり途中保存しない。探索再開の初期化を通常の地図復元より前に設計し、通常画面で移動できるのに予約だけ残る状態を防ぐ。
- 戦闘開始前の装備・兵員・敵を復帰用に固定し、再開時の二重日数消費や報酬再抽選を防ぐ。
- `time.js` は日次ループ内の季節1日に部隊・船維持費を処理してから探索地点の更新へ進む。複数季節をまとめて進めても、各季節境界で支払う。探索は1日単位で呼び、保存失敗時は船の自動売却も状態全体とともに巻き戻す。

### R9. 調整用の初期値（実装開始時に初期案を採用）

- 自然探索の共通上限5件、難破船2件。漂流物／戦場跡の海陸比率50:50。
- 救助兵種は全兵種から兵員1人ごとに均等抽選、全員Lv1、合計3～5人。
- 資金450～550／900～1100、高級品2種類合計2～4／4～8個。2種類とも最低1個確保後、残りを均等配分抽選。
- 海図は信仰90～110／180～220、名声9～11／18～22の整数一様抽選。高級品は各種最低1個＋残りを5種類均等に配分し、合計100／200。入り江の船は2／4隻、財宝は25000／50000資金。探索開始済みの保存報酬は再抽選せず保持する。
- 艤装の購入・付け替え・枠拡張では日数を消費しない。購入済み設備の売却はなし、付け替え無料。

### R10. 調整時に確認する体験（仕様変更はしない）

- 0.5%は対象となる勝利200回で平均1枚、3%は対象となる依頼候補約33件で平均1件。保証ではなく、予約上限による除外もある。3枚版・5枚版の両方が長期間未完のままになる可能性を通しプレイで確認する。
- 無期限の噂地点は遠方でも残るため、依頼カードからの場所強調と取得済み／予約済みの区別が重要。
- 艤装の比較は保有兵員の現状から算出する。救命・見張りが上限なら増分0、物資や兵員上限が減る交換では超過量を示す。戦闘用バフは地形未確定時の値を確定値のように表示しない。
- 海図の大きな報酬で上限を超えても受領額を切り捨てない。船加算後の上限と現在量を同じ結果画面に示し、整理への導線を確保する。

R1～R5は承認済み。R6～R8には資金報酬の上書き防止、予約の整合性、報酬源別の抽選、日次イベントと途中再開の注意を記載済み。実装前に必ず参照する。R9は実装開始時に初期値として採用した。調整は `expansionConfig.js` を通して行い、本書の実装進捗も更新する。
